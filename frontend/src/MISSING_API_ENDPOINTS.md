# ⚠️ НЕДОСТАЮЩИЕ API ЭНДПОИНТЫ

## 🔴 КРИТИЧЕСКИ ВАЖНЫЕ (Без них приложение не будет работать полноценно)

### 1. **Публичное получение страницы по короткому коду**
**Проблема:** Когда пользователь сканирует QR-код, нужно показать страницу БЕЗ авторизации.

**Требуемый эндпоинт:**
```
GET /public/page/{short_code}/
```

**Описание:** Получение страницы по короткому коду (например, "abc123") для публичного просмотра

**Пример запроса:**
```
GET http://localhost:9000/public/page/abc123/
```

**Ожидаемый ответ:**
```json
{
  "page": {
    "id": "uuid",
    "title": "Моя страница",
    "content": { ... },
    "published": true
  },
  "qr_code": {
    "id": "uuid",
    "short_code": "abc123",
    "qr_url": "https://qrwear.com/u/abc123"
  }
}
```

**Использование:**
- При сканировании QR-кода пользователь переходит на `/u/abc123`
- Frontend делает запрос `GET /public/page/abc123/`
- Отображается страница с контентом
- **БЕЗ АВТОРИЗАЦИИ**

---

### 2. **Регистрация сканирования (аналитика)**
**Проблема:** Нужно отслеживать сканирования QR-кодов для статистики.

**Требуемый эндпоинт:**
```
POST /public/scan/{short_code}/
```

**Описание:** Регистрация факта сканирования QR-кода

**Пример запроса:**
```
POST http://localhost:9000/public/scan/abc123/
Content-Type: application/json

{
  "user_agent": "Mozilla/5.0...",
  "referrer": "https://example.com",
  "device_type": "mobile"
}
```

**Ожидаемый ответ:**
```json
{
  "message": "Scan registered",
  "scan_count": 145
}
```

**Использование:**
- Когда пользователь открывает страницу по QR-коду
- Frontend автоматически отправляет POST запрос
- Увеличивается счетчик scan_count
- Сохраняется информация об устройстве, браузере, времени

---

### 3. **Загрузка файлов (изображения, видео)**
**Проблема:** Пользователи должны загружать изображения для страниц, аватары, логотипы для QR-кодов.

**Требуемые эндпоинты:**

#### a) Загрузка файла
```
POST /media/upload/
```

**Пример запроса:**
```
POST http://localhost:9000/media/upload/
Content-Type: multipart/form-data

file: <binary>
folder: "avatars" | "qr-logos" | "page-images" | "page-videos"
```

**Ожидаемый ответ:**
```json
{
  "url": "https://storage.example.com/uploads/page-images/abc123.jpg",
  "size": 245678,
  "mime_type": "image/jpeg",
  "filename": "abc123.jpg"
}
```

#### b) Удаление файла
```
DELETE /media/{file_id}/
```

**Пример запроса:**
```
DELETE http://localhost:9000/media/abc123.jpg/
```

**Использование:**
- При создании блока с изображением
- При загрузке аватара пользователя
- При добавлении логотипа на QR-код

---

### 4. **Обновление профиля пользователя**
**Проблема:** У вас есть GET /user/me/, но нет способа обновить данные профиля.

**Требуемый эндпоинт:**
```
PATCH /user/me/
```

**Описание:** Обновление данных текущего пользователя

**Пример запроса:**
```
PATCH http://localhost:9000/user/me/
Content-Type: application/json

{
  "username": "newusername",
  "avatar_url": "https://storage.../avatar.jpg"
}
```

**Ожидаемый ответ:**
```json
{
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "username": "newusername",
    "avatar_url": "https://storage.../avatar.jpg",
    "subscription_tier": "free",
    "updated_at": "2025-12-07T..."
  }
}
```

---

## 🟡 ВАЖНЫЕ (Желательно добавить для полноценной работы)

### 5. **Статистика и аналитика**

#### a) Статистика по QR-коду
```
GET /analytics/qr/{qr_id}/?period=7d
```

**Ожидаемый ответ:**
```json
{
  "qr_code_id": "uuid",
  "period": "7d",
  "total_scans": 245,
  "scans_by_day": [
    { "date": "2025-12-01", "count": 12 },
    { "date": "2025-12-02", "count": 8 }
  ],
  "devices": {
    "mobile": 180,
    "tablet": 25,
    "desktop": 40
  },
  "top_locations": [
    { "country": "Russia", "count": 150 },
    { "country": "USA", "count": 45 }
  ]
}
```

#### b) Общая статистика пользователя
```
GET /analytics/dashboard/
```

**Ожидаемый ответ:**
```json
{
  "overview": {
    "total_qr_codes": 5,
    "total_scans": 1234,
    "scans_today": 23,
    "scans_this_week": 156
  },
  "top_qr_codes": [
    {
      "qr_code": { ... },
      "scan_count": 456
    }
  ],
  "recent_scans": [ ... ]
}
```

---

### 6. **Подписки (Subscription)**

#### a) Получить планы подписок
```
GET /subscription/plans/
```

**Ожидаемый ответ:**
```json
{
  "plans": [
    {
      "tier": "free",
      "name": "Бесплатный",
      "price": 0,
      "limits": {
        "qr_codes": 3,
        "custom_styles": false,
        "analytics": "basic"
      }
    },
    {
      "tier": "basic",
      "name": "Базовый",
      "price": 490,
      "currency": "RUB",
      "billing_period": "month",
      "limits": {
        "qr_codes": 10,
        "custom_styles": true,
        "analytics": "advanced"
      }
    }
  ]
}
```

#### b) Создать подписку (checkout)
```
POST /subscription/checkout/
```

**Пример запроса:**
```json
{
  "tier": "basic",
  "billing_period": "month"
}
```

**Ожидаемый ответ:**
```json
{
  "checkout_url": "https://payment-provider.com/checkout/...",
  "session_id": "cs_..."
}
```

#### c) Отменить подписку
```
POST /subscription/cancel/
```

---

### 7. **Получение страницы по QR ID**
**Проблема:** Сейчас можно получить страницу только по page_id, но удобнее получать по qr_code_id.

**Требуемый эндпоинт:**
```
GET /page/by-qr/{qr_code_id}/
```

**Описание:** Получение страницы, привязанной к QR-коду

**Ожидаемый ответ:**
```json
{
  "page": { ... }
}
```

**Использование:**
- В редакторе страницы, когда есть только ID QR-кода
- Быстрое получение страницы без дополнительных запросов

---

### 8. **Поиск и фильтрация QR-кодов**

```
GET /qr/?search=футболка&status=active&sort=scan_count&order=desc
```

**Query параметры:**
- `search` - поиск по названию
- `status` - фильтр по статусу (active/inactive)
- `sort` - сортировка (created_at, scan_count, name)
- `order` - порядок (asc/desc)
- `page` - номер страницы
- `limit` - кол-во на странице

---

## 🟢 ДОПОЛНИТЕЛЬНЫЕ (Nice to have)

### 9. **Дублирование QR-кода**
```
POST /qr/{qr_id}/duplicate/
```

**Описание:** Создает копию QR-кода со всеми настройками

---

### 10. **Экспорт/Импорт страницы**

#### Экспорт
```
GET /page/{page_id}/export/
```

**Ответ:** JSON файл со всем контентом страницы

#### Импорт
```
POST /page/import/
```

**Тело:** JSON с контентом страницы

---

### 11. **Смена пароля**
```
POST /user/change-password/
```

**Пример запроса:**
```json
{
  "old_password": "OldPassword123!",
  "new_password": "NewPassword123!"
}
```

---

### 12. **Восстановление пароля**

#### Запрос восстановления
```
POST /user/forgot-password/
```

**Пример запроса:**
```json
{
  "email": "user@example.com"
}
```

#### Сброс пароля
```
POST /user/reset-password/
```

**Пример запроса:**
```json
{
  "token": "reset-token-from-email",
  "new_password": "NewPassword123!"
}
```

---

### 13. **Массовые операции**

#### Удаление нескольких QR-кодов
```
POST /qr/bulk-delete/
```

**Пример запроса:**
```json
{
  "qr_ids": ["uuid1", "uuid2", "uuid3"]
}
```

#### Активация/деактивация нескольких QR-кодов
```
POST /qr/bulk-update/
```

**Пример запроса:**
```json
{
  "qr_ids": ["uuid1", "uuid2"],
  "is_active": false
}
```

---

## 📊 ИТОГОВАЯ ТАБЛИЦА ПРИОРИТЕТОВ

| Приоритет | Эндпоинт | Критичность | Зачем нужен |
|-----------|----------|-------------|-------------|
| 🔴 **1** | `GET /public/page/{short_code}/` | **КРИТИЧНО** | Просмотр страницы после сканирования QR |
| 🔴 **2** | `POST /public/scan/{short_code}/` | **КРИТИЧНО** | Регистрация сканирований для статистики |
| 🔴 **3** | `POST /media/upload/` | **КРИТИЧНО** | Загрузка изображений/видео |
| 🔴 **4** | `PATCH /user/me/` | **КРИТИЧНО** | Обновление профиля пользователя |
| 🟡 **5** | `GET /analytics/qr/{qr_id}/` | **ВАЖНО** | Статистика по QR-коду |
| 🟡 **6** | `GET /analytics/dashboard/` | **ВАЖНО** | Общая статистика пользователя |
| 🟡 **7** | `GET /subscription/plans/` | **ВАЖНО** | Список планов подписок |
| 🟡 **8** | `POST /subscription/checkout/` | **ВАЖНО** | Оформление подписки |
| 🟡 **9** | `GET /page/by-qr/{qr_code_id}/` | **ВАЖНО** | Получение страницы по QR ID |
| 🟢 **10** | `POST /user/change-password/` | Желательно | Смена пароля |
| 🟢 **11** | `POST /user/forgot-password/` | Желательно | Восстановление пароля |
| 🟢 **12** | `POST /qr/{qr_id}/duplicate/` | Желательно | Дублирование QR-кода |
| 🟢 **13** | `DELETE /media/{file_id}/` | Желательно | Удаление загруженных файлов |

---

## 💡 РЕКОМЕНДАЦИИ

### Первоочередная реализация (MVP):
1. ✅ `GET /public/page/{short_code}/` - **БЕЗ ЭТОГО НЕ РАБОТАЕТ СКАНИРОВАНИЕ**
2. ✅ `POST /public/scan/{short_code}/` - **БЕЗ ЭТОГО НЕТ СТАТИСТИКИ**
3. ✅ `POST /media/upload/` - **БЕЗ ЭТОГО НЕТ ИЗОБРАЖЕНИЙ**
4. ✅ `PATCH /user/me/` - **БЕЗ ЭТОГО НЕЛЬЗЯ РЕДАКТИРОВАТЬ ПРОФИЛЬ**

### Вторая волна (Полноценный продукт):
5. `GET /analytics/qr/{qr_id}/`
6. `GET /analytics/dashboard/`
7. `GET /page/by-qr/{qr_code_id}/`

### Третья волна (Монетизация):
8. `GET /subscription/plans/`
9. `POST /subscription/checkout/`
10. `POST /subscription/cancel/`

### Четвертая волна (Улучшения UX):
11. Смена/восстановление пароля
12. Дублирование QR-кодов
13. Массовые операции

---

## 🚀 НАЧАТЬ С ЭТОГО:

Минимальный набор для рабочего приложения:

```
1. GET  /public/page/{short_code}/     - Публичный просмотр страницы
2. POST /public/scan/{short_code}/     - Учет сканирований
3. POST /media/upload/                 - Загрузка файлов
4. PATCH /user/me/                     - Обновление профиля
```

**Всё остальное может подождать, но эти 4 эндпоинта критичны!** 🎯
