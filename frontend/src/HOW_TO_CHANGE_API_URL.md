# 🔧 Как изменить базовый URL API

## Простая инструкция

Базовый URL API задаётся **в одном месте** и автоматически применяется ко всем запросам.

---

## 📍 Где находится:

**Файл:** `/services/api.ts`  
**Строка:** 13

---

## 🔄 Как изменить:

### 1. Откройте файл `/services/api.ts`

### 2. Найдите эту строку (строка 13):

```typescript
export const API_BASE_URL = 'http://localhost:9000';
```

### 3. Измените URL на нужный:

#### Локальная разработка (по умолчанию):
```typescript
export const API_BASE_URL = 'http://localhost:9000';
```

#### Production сервер:
```typescript
export const API_BASE_URL = 'https://api.your-domain.com';
```

#### Другой локальный порт:
```typescript
export const API_BASE_URL = 'http://localhost:8000';
```

#### Внешний тестовый сервер:
```typescript
export const API_BASE_URL = 'https://staging-api.your-domain.com';
```

### 4. Сохраните файл

**Готово!** ✅ Все компоненты автоматически будут использовать новый URL.

---

## 💡 С переменными окружения (опционально)

Если хотите управлять URL через `.env` файл:

### 1. Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:9000
```

### 2. Измените строку 13 в `/services/api.ts`:

```typescript
export const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:9000';
```

### 3. Для production создайте `.env.production`:

```env
VITE_API_URL=https://api.your-domain.com
```

### 4. Перезапустите dev-сервер:

```bash
npm run dev
```

---

## 🧪 Проверка

После изменения URL откройте консоль браузера (F12) и увидите:

```
🌐 API Request: POST /user/login/
📤 URL: http://ВАШ-НОВЫЙ-URL/user/login/    ← Здесь будет новый URL
📦 Data: { ... }
```

---

## ⚠️ Важно:

1. **Слэш в конце НЕ нужен:** ✅ `http://localhost:9000` (правильно)
2. **Без слэша:** ❌ `http://localhost:9000/` (неправильно)
3. **Без `/api` в конце** (если ваш backend не требует)

---

## 📝 Примеры использования

### Разработка на localhost:
```typescript
export const API_BASE_URL = 'http://localhost:9000';
```

### Разработка с Docker:
```typescript
export const API_BASE_URL = 'http://backend:9000';
```

### Production на Vercel/Netlify + Railway:
```typescript
export const API_BASE_URL = 'https://your-app.railway.app';
```

### Production на своём домене:
```typescript
export const API_BASE_URL = 'https://api.qrwear.app';
```

---

## 🚀 Готово!

Теперь все 23 API эндпоинта будут использовать новый базовый URL автоматически!

**Изменили URL?** → Перезагрузите страницу → Готово! ✨
