# 📱 Публичная страница - Руководство

## Что это?

**PublicPage** - это страница, которая отображается когда пользователь сканирует QR-код. Она работает **БЕЗ АВТОРИЗАЦИИ** и доступна всем.

---

## 🎯 Как работает система

### 1. **Пользователь создает QR-код**
```
Dashboard → Создать QR → Настроить стиль → Сохранить
```
Backend возвращает:
```json
{
  "qr_code": {
    "id": "uuid-123",
    "short_code": "abc123",
    "qr_url": "https://qrwear.com/u/abc123"
  }
}
```

### 2. **Пользователь создает страницу для QR-кода**
```
Dashboard → Редактировать страницу → Добавить блоки → Опубликовать
```

### 3. **QR-код печатается на одежде**
QR-код содержит ссылку: `https://qrwear.com/u/abc123`

### 4. **Кто-то сканирует QR-код**
При сканировании происходит:

**Frontend:**
```typescript
// Пользователь переходит на /u/abc123
// App.tsx вызывает:
navigateToPublicPage('abc123');
```

**PublicPage загружается и делает 2 запроса:**

**a) Загрузка страницы:**
```typescript
GET http://localhost:9000/public/page/abc123/

// Ответ:
{
  "page": {
    "title": "Моя страница",
    "content": {
      "theme": { ... },
      "blocks": [ ... ]
    },
    "published": true
  },
  "qr_code": {
    "short_code": "abc123",
    "scan_count": 145
  }
}
```

**b) Регистрация сканирования:**
```typescript
POST http://localhost:9000/public/scan/abc123/
{
  "user_agent": "Mozilla/5.0...",
  "device_type": "mobile"
}

// Ответ:
{
  "message": "Scan registered",
  "scan_count": 146
}
```

---

## 📋 Структура PublicPage

### Основные элементы:

1. **Шапка (Header)**
   - Логотип QR Wear (слева)
   - Кнопка "Войти" (справа)
   - Фиксированная, прозрачная с blur эффектом

2. **Фон (Background)**
   - Поддерживает 4 типа:
     - `color` - сплошной цвет
     - `gradient` - градиент
     - `image` - фоновое изображение
     - `video` - фоновое видео

3. **Заголовок страницы**
   - Название (`page.title`)
   - Описание (`page.description`)

4. **Блоки контента**
   - Отображаются в порядке `block.order`
   - Только видимые блоки (`block.visible = true`)

5. **Футер (Footer)**
   - "Создано с помощью QR Wear"
   - Кнопка "Создать свою страницу"

---

## 🎨 Поддерживаемые типы блоков

### 1. **Text** - Текстовый блок
```json
{
  "type": "text",
  "data": {
    "text": "Привет! Я Мария 👋",
    "alignment": "center",
    "fontSize": "large",
    "fontWeight": "bold",
    "color": "#ffffff"
  }
}
```

### 2. **Image** - Изображение
```json
{
  "type": "image",
  "data": {
    "url": "https://...",
    "alt": "Описание",
    "caption": "Подпись",
    "height": "400px",
    "fit": "cover"
  }
}
```

### 3. **Video** - Видео (YouTube, Vimeo)
```json
{
  "type": "video",
  "data": {
    "url": "https://youtube.com/embed/...",
    "caption": "Мое видео"
  }
}
```

### 4. **Social Links** - Соцсети
```json
{
  "type": "social_links",
  "data": {
    "layout": "icons | buttons | grid",
    "links": [
      {
        "platform": "instagram",
        "url": "https://instagram.com/maria",
        "username": "@maria",
        "color": "#E4405F"
      }
    ]
  }
}
```

Поддерживаемые платформы:
- Instagram, Twitter, Facebook, Youtube, Linkedin
- Website, Email, Phone

### 5. **Link Button** - Кнопка-ссылка
```json
{
  "type": "link_button",
  "data": {
    "text": "Мой сайт",
    "url": "https://example.com",
    "backgroundColor": "#7c6afa",
    "textColor": "#ffffff",
    "icon": "🌐"
  }
}
```

### 6. **Gallery** - Галерея
```json
{
  "type": "gallery",
  "data": {
    "columns": 3,
    "images": [
      { "url": "https://...", "alt": "Photo 1" }
    ]
  }
}
```

### 7. **Divider** - Разделитель
```json
{
  "type": "divider",
  "data": {
    "thickness": "2px",
    "color": "#ffffff",
    "opacity": 0.3
  }
}
```

### 8. **Spacer** - Пустое пространство
```json
{
  "type": "spacer",
  "data": {
    "height": "40px"
  }
}
```

### 9. **Contact Form** - Форма связи
```json
{
  "type": "contact_form",
  "data": {
    "title": "Связаться со мной"
  }
}
```

### 10. **Embed** - Встроенный код
```json
{
  "type": "embed",
  "data": {
    "code": "<iframe>...</iframe>"
  }
}
```

---

## 🔧 Использование в коде

### Переход на публичную страницу:

```typescript
// Из любого компонента:
navigateToPublicPage('abc123');

// Или напрямую в App.tsx:
setPublicShortCode('abc123');
setCurrentPage('public-page');
```

### Пример использования API:

```typescript
import api from './services/api';

// Загрузка страницы
const response = await api.public.getPageByShortCode('abc123');
console.log(response.data.page);

// Регистрация сканирования
await api.public.registerScan('abc123', {
  user_agent: navigator.userAgent,
  device_type: 'mobile'
});
```

---

## 🎯 Особенности

### ✅ Что работает:

1. **Загрузка без авторизации** - любой может открыть страницу
2. **Автоматический учет сканирований** - каждое открытие регистрируется
3. **Адаптивный дизайн** - работает на всех устройствах
4. **Анимации** - если включены в настройках страницы
5. **SEO** - meta-теги из `page.content.seo`
6. **Кастомизация темы** - цвета, фоны, шрифты

### ⚠️ Важно:

1. **Страница должна быть опубликована** (`published: true`)
2. **Short code должен быть уникальным**
3. **QR-код должен быть активным** (`is_active: true`)

---

## 🚀 Сценарий использования

### Полный цикл:

1. **Регистрация пользователя**
   ```typescript
   await api.user.register({ ... });
   ```

2. **Создание QR-кода**
   ```typescript
   const qr = await api.qr.create({ name: 'My T-Shirt' });
   // qr.data.qr_code.short_code = 'abc123'
   ```

3. **Создание страницы**
   ```typescript
   await api.page.create({
     qr_code_id: qr.data.qr_code.id,
     title: 'My Page',
     content: { ... }
   });
   ```

4. **Печать QR на одежде**
   - QR содержит: `https://qrwear.com/u/abc123`

5. **Пользователь сканирует QR**
   - Открывается PublicPage
   - Загружается контент из backend
   - Регистрируется сканирование
   - Показывается красивая страница

6. **Владелец видит статистику**
   - Dashboard → Статистика
   - Количество сканирований: 146
   - Устройства, география, время

---

## 🎨 Примеры страниц

### Простая страница с текстом и соцсетями:

```json
{
  "title": "Привет, я Мария!",
  "description": "Фотограф из Москвы",
  "content": {
    "theme": {
      "background": {
        "type": "gradient",
        "value": "linear-gradient(135deg, #7c6afa, #c89afc)"
      },
      "textColor": "#ffffff",
      "accentColor": "#df5950"
    },
    "blocks": [
      {
        "id": "1",
        "type": "text",
        "order": 0,
        "visible": true,
        "data": {
          "text": "📸 Профессиональная фотография",
          "alignment": "center",
          "fontSize": "large"
        }
      },
      {
        "id": "2",
        "type": "social_links",
        "order": 1,
        "visible": true,
        "data": {
          "layout": "icons",
          "links": [
            {
              "platform": "instagram",
              "url": "https://instagram.com/maria",
              "color": "#E4405F"
            }
          ]
        }
      }
    ]
  }
}
```

---

## 🔒 Безопасность

- ✅ **XSS защита** - используется `dangerouslySetInnerHTML` только для embed блоков
- ✅ **CORS** - настроен в backend для публичных эндпоинтов
- ✅ **Rate limiting** - защита от спама сканирований
- ✅ **Validation** - все данные валидируются на backend

---

Готово! 🎉 Теперь у вас есть полноценная система публичных страниц для QR-кодов!
