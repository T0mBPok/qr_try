# 🏗️ АРХИТЕКТУРА ПРОЕКТА - Полный обзор

## 📊 Общая схема

```
┌─────────────────────────────────────────────────────────────────┐
│                         ПОЛЬЗОВАТЕЛЬ                             │
│                    (Браузер / Мобильный)                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                            │
│                   http://localhost:5173                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Homepage   │  │     Auth     │  │  Dashboard   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  QRCreator   │  │ PageEditor   │  │ FileUploader │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ PublicPage   │  │ Subscription │  │   Examples   │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│                          ▼                                       │
│                   ┌─────────────┐                                │
│                   │ /services/  │                                │
│                   │   api.ts    │ ◄─ API Service Layer           │
│                   └─────────────┘                                │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP Requests (JSON)
                         │ JWT Auth (Cookies)
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     BACKEND (Python/Node)                        │
│                   http://localhost:9000                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                    API ENDPOINTS                        │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │                                                         │    │
│  │  User API (5 endpoints)                                │    │
│  │  ├─ POST   /user/register/                             │    │
│  │  ├─ POST   /user/login/                                │    │
│  │  ├─ GET    /user/me/                                   │    │
│  │  ├─ GET    /user/check/                                │    │
│  │  └─ POST   /user/logout/                               │    │
│  │                                                         │    │
│  │  QR Code API (5 endpoints)                             │    │
│  │  ├─ POST   /qr/                                        │    │
│  │  ├─ GET    /qr/                                        │    │
│  │  ├─ GET    /qr/{qr_id}/                                │    │
│  │  ├─ PUT    /qr/{qr_id}/                                │    │
│  │  └─ DELETE /qr/{qr_id}/                                │    │
│  │                                                         │    │
│  │  Page API (9 endpoints)                                │    │
│  │  ├─ POST   /page/                                      │    │
│  │  ├─ GET    /page/                                      │    │
│  │  ├─ GET    /page/{page_id}/                            │    │
│  │  ├─ PUT    /page/{page_id}/                            │    │
│  │  ├─ DELETE /page/{page_id}/                            │    │
│  │  ├─ POST   /page/{page_id}/files/        ◄─ NEW!       │    │
│  │  ├─ GET    /page/{page_id}/files/        ◄─ NEW!       │    │
│  │  ├─ GET    /page/{page_id}/files/{name}/ ◄─ NEW!       │    │
│  │  └─ DELETE /page/{page_id}/files/{name}/ ◄─ NEW!       │    │
│  │                                                         │    │
│  │  Public API (2 endpoints, NO AUTH)                     │    │
│  │  ├─ GET    /public/page/{short_code}/                  │    │
│  │  └─ POST   /public/scan/{short_code}/                  │    │
│  │                                                         │    │
│  └────────────────────────────────────────────────────────┘    │
│                          ▼                                       │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              BUSINESS LOGIC LAYER                       │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  - JWT Authentication                                   │    │
│  │  - Data Validation                                      │    │
│  │  - QR Code Generation                                   │    │
│  │  - File Upload/Management                               │    │
│  │  - Scan Tracking                                        │    │
│  └────────────────────────────────────────────────────────┘    │
│                          ▼                                       │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                  DATABASE LAYER                         │    │
│  └────────────────────────────────────────────────────────┘    │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     DATABASE (PostgreSQL)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐          │
│  │    users    │   │  qr_codes   │   │    pages    │          │
│  ├─────────────┤   ├─────────────┤   ├─────────────┤          │
│  │ id          │   │ id          │   │ id          │          │
│  │ email       │   │ user_id     │◄──│ qr_code_id  │          │
│  │ username    │◄──│ name        │   │ title       │          │
│  │ password    │   │ short_code  │   │ content     │          │
│  │ tier        │   │ qr_style    │   │ published   │          │
│  └─────────────┘   │ scan_count  │   └─────────────┘          │
│                    │ is_active   │                              │
│                    └─────────────┘                              │
│                          │                                       │
│                          │                                       │
│  ┌─────────────┐        │         ┌─────────────┐              │
│  │    scans    │◄───────┘         │    files    │              │
│  ├─────────────┤                  ├─────────────┤              │
│  │ id          │                  │ id          │              │
│  │ qr_code_id  │                  │ page_id     │              │
│  │ scanned_at  │                  │ filename    │              │
│  │ user_agent  │                  │ url         │              │
│  │ device_type │                  │ size        │              │
│  │ referrer    │                  │ uploaded_at │              │
│  └─────────────┘                  └─────────────┘              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FILE STORAGE (Disk)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  /uploads/                                                       │
│    ├─ {user_id_1}/                                              │
│    │   ├─ {page_id_1}/                                          │
│    │   │   ├─ photo1.jpg                                        │
│    │   │   ├─ video1.mp4                                        │
│    │   │   └─ document.pdf                                      │
│    │   └─ {page_id_2}/                                          │
│    │       └─ ...                                                │
│    └─ {user_id_2}/                                              │
│        └─ ...                                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 ПОТОКИ ДАННЫХ

### 1. Регистрация пользователя

```
User Input → Auth Component → api.user.register()
    ↓
POST /user/register/
    ↓
Backend validates & creates user
    ↓
JWT token set in cookie
    ↓
Response → Frontend updates state
    ↓
Redirect to Dashboard
```

### 2. Создание QR-кода

```
User Input → QRCreator Component → api.qr.create()
    ↓
POST /qr/
    ↓
Backend:
  - Generates unique short_code
  - Creates QR image
  - Saves to database
    ↓
Response: { qr_code, qr_image_url }
    ↓
Frontend displays QR
```

### 3. Создание и публикация страницы

```
User Input → PageEditor Component → api.page.create()
    ↓
POST /page/
    ↓
Backend saves page with content
    ↓
User adds blocks (text, images, etc.)
    ↓
User uploads media → FileUploader → api.page.uploadFiles()
    ↓
POST /page/{page_id}/files/
    ↓
Backend saves files to disk
    ↓
User publishes → api.page.update({ published: true })
    ↓
Page is now public!
```

### 4. Сканирование QR-кода

```
QR Code scanned → Browser opens URL
    ↓
https://qrwear.com/u/abc123
    ↓
Frontend: PublicPage Component
    ↓
api.public.getPageByShortCode('abc123')
    ↓
GET /public/page/abc123/ (NO AUTH)
    ↓
Backend returns page content
    ↓
api.public.registerScan('abc123')
    ↓
POST /public/scan/abc123/
    ↓
Backend:
  - Increments scan_count
  - Saves scan record (device, time, etc.)
    ↓
Frontend displays page
```

### 5. Загрузка файлов

```
User drags files → FileUploader Component
    ↓
api.page.uploadFiles(pageId, files[])
    ↓
POST /page/{page_id}/files/
Content-Type: multipart/form-data
    ↓
Backend:
  - Validates file types & sizes
  - Saves to /uploads/{user_id}/{page_id}/
  - Generates public URLs
  - Updates database
    ↓
Response: [{ filename, url, size }, ...]
    ↓
Frontend displays files list
    ↓
User clicks "Use" → URL inserted into page block
```

---

## 🔐 АВТОРИЗАЦИЯ

### JWT Flow

```
┌─────────────────────────────────────────────────────────┐
│                    1. LOGIN                              │
└─────────────────────────────────────────────────────────┘
                         │
POST /user/login/        │
{ email, password }      │
                         ▼
             ┌───────────────────────┐
             │  Backend validates    │
             │  credentials          │
             └───────────────────────┘
                         │
                         ▼
             ┌───────────────────────┐
             │  Generate JWT token   │
             │  { user_id, email }   │
             └───────────────────────┘
                         │
                         ▼
             ┌───────────────────────┐
             │  Set HTTP-only cookie │
             │  access_token=JWT     │
             └───────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│              2. AUTHENTICATED REQUESTS                   │
└─────────────────────────────────────────────────────────┘
                         │
GET /qr/                 │  (with cookie)
                         ▼
             ┌───────────────────────┐
             │  Backend reads cookie │
             │  Verifies JWT         │
             └───────────────────────┘
                         │
                         ▼
             ┌───────────────────────┐
             │  Extract user_id      │
             │  from JWT payload     │
             └───────────────────────┘
                         │
                         ▼
             ┌───────────────────────┐
             │  Fetch user's data    │
             │  from database        │
             └───────────────────────┘
                         │
                         ▼
             ┌───────────────────────┐
             │  Return response      │
             └───────────────────────┘
```

---

## 📦 СТРУКТУРА ФАЙЛОВ

### Frontend

```
/
├── App.tsx                      # Main app, routing
├── services/
│   └── api.ts                   # API service (23 endpoints)
├── components/
│   ├── Homepage.tsx             # Landing page
│   ├── Auth.tsx                 # Login/Register
│   ├── Dashboard.tsx            # User dashboard
│   ├── QRCreator.tsx            # QR code creator
│   ├── QRSettings.tsx           # QR settings editor
│   ├── PageEditor.tsx           # Page content editor
│   ├── FileUploader.tsx         # File upload component ⭐ NEW
│   ├── PublicPage.tsx           # Public page viewer ⭐ NEW
│   ├── Subscription.tsx         # Subscription plans
│   ├── Examples.tsx             # Examples page
│   ├── Instructions.tsx         # Instructions
│   └── Contacts.tsx             # Contacts page
├── styles/
│   └── globals.css              # Global styles
└── imports/                     # Figma assets
```

### Backend (нужно создать)

```
/
├── main.py / index.js           # Entry point
├── routes/
│   ├── user.py                  # User routes (5 endpoints)
│   ├── qr.py                    # QR routes (5 endpoints)
│   ├── page.py                  # Page routes (9 endpoints)
│   └── public.py                # Public routes (2 endpoints)
├── models/
│   ├── user.py                  # User model
│   ├── qr_code.py               # QR Code model
│   ├── page.py                  # Page model
│   ├── scan.py                  # Scan model
│   └── file.py                  # File model
├── middleware/
│   ├── auth.py                  # JWT verification
│   └── validation.py            # Input validation
├── utils/
│   ├── jwt.py                   # JWT helpers
│   ├── qr_generator.py          # QR code generation
│   └── file_handler.py          # File upload handling
├── uploads/                     # File storage
│   └── {user_id}/
│       └── {page_id}/
└── database.py                  # Database connection
```

---

## 🎨 КОМПОНЕНТЫ

### Основные

| Компонент | Назначение | API методы |
|-----------|-----------|------------|
| `Auth` | Регистрация/вход | `api.user.register()`, `api.user.login()` |
| `Dashboard` | Управление QR | `api.qr.getAll()`, `api.qr.delete()` |
| `QRCreator` | Создание QR | `api.qr.create()` |
| `QRSettings` | Редактирование QR | `api.qr.getById()`, `api.qr.update()` |
| `PageEditor` | Редактор страниц | `api.page.getById()`, `api.page.update()` |
| `FileUploader` | Загрузка медиа | `api.page.uploadFiles()`, `api.page.getFiles()` |
| `PublicPage` | Публичный просмотр | `api.public.getPageByShortCode()` |

### Вспомогательные

| Компонент | Назначение |
|-----------|-----------|
| `Homepage` | Главная страница |
| `Subscription` | Тарифные планы |
| `Examples` | Примеры использования |
| `Instructions` | Инструкции |
| `Contacts` | Контакты |

---

## 🔄 ЖИЗНЕННЫЙ ЦИКЛ QR-КОДА

```
┌─────────────────────────────────────────────────────────────────┐
│  1. СОЗДАНИЕ                                                     │
├─────────────────────────────────────────────────────────────────┤
│  User → QRCreator → api.qr.create()                             │
│    Backend:                                                      │
│    - Generate unique short_code (6 chars)                       │
│    - Generate QR image with style                                │
│    - Create database record                                      │
│    Return: { qr_code, qr_image_url }                            │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  2. СОЗДАНИЕ СТРАНИЦЫ                                            │
├─────────────────────────────────────────────────────────────────┤
│  User → PageEditor → api.page.create()                          │
│    Backend:                                                      │
│    - Create page linked to QR code                              │
│    - Save initial content (empty blocks)                         │
│    Return: { page }                                             │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  3. ДОБАВЛЕНИЕ КОНТЕНТА                                          │
├─────────────────────────────────────────────────────────────────┤
│  User adds blocks:                                               │
│    - Text blocks                                                 │
│    - Image blocks (via FileUploader)                            │
│    - Video blocks                                                │
│    - Social links                                                │
│    - Buttons                                                     │
│    - Gallery                                                     │
│    - Forms                                                       │
│                                                                  │
│  Each change → api.page.update()                                │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  4. ПУБЛИКАЦИЯ                                                   │
├─────────────────────────────────────────────────────────────────┤
│  User clicks "Publish" → api.page.update({ published: true })  │
│    Page is now publicly accessible                              │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  5. ПЕЧАТЬ QR-КОДА                                               │
├─────────────────────────────────────────────────────────────────┤
│  User downloads QR image                                         │
│  Prints on t-shirt/product                                       │
│  QR contains: https://qrwear.com/u/{short_code}                 │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  6. СКАНИРОВАНИЕ                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Someone scans QR → Opens URL                                    │
│    Frontend:                                                     │
│    - PublicPage loads                                            │
│    - api.public.getPageByShortCode()                            │
│    - api.public.registerScan()                                  │
│                                                                  │
│    Backend:                                                      │
│    - Return page content                                         │
│    - Increment scan_count                                        │
│    - Save scan record (time, device, location)                  │
└─────────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│  7. АНАЛИТИКА                                                    │
├─────────────────────────────────────────────────────────────────┤
│  Owner → Dashboard → sees statistics:                            │
│    - Total scans                                                 │
│    - Scans over time                                             │
│    - Device types                                                │
│    - Geographic data                                             │
│    - Referrers                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 DEPLOYMENT

### Frontend

```bash
# Build
npm run build

# Output: /dist/

# Deploy to:
- Vercel
- Netlify
- AWS S3 + CloudFront
- Any static hosting
```

### Backend

```bash
# Python (FastAPI)
uvicorn main:app --host 0.0.0.0 --port 9000

# Node.js (Express)
node server.js

# Deploy to:
- Heroku
- AWS EC2 / Lambda
- DigitalOcean
- Railway
```

### Database

```sql
-- PostgreSQL
CREATE DATABASE qrwear;

-- Tables
CREATE TABLE users (...);
CREATE TABLE qr_codes (...);
CREATE TABLE pages (...);
CREATE TABLE scans (...);
CREATE TABLE files (...);
```

---

## 📊 МАСШТАБИРОВАНИЕ

### Уровни производительности

| Компонент | Бесплатно | Basic | Premium |
|-----------|-----------|-------|---------|
| QR-коды | 3 | 10 | Безлимит |
| Сканирования | 100/мес | 1000/мес | Безлимит |
| Загрузка файлов | 10MB | 100MB | 1GB |
| Кастомный домен | ❌ | ❌ | ✅ |
| Аналитика | Базовая | Расширенная | Полная |

---

## 🎯 ИТОГ

### Готово к работе:

✅ Frontend - 100%  
✅ API интерфейс - 100%  
✅ Компоненты - 100%  
✅ Типизация - 100%  
✅ Документация - 100%  

### Требуется:

⏳ Backend implementation  
⏳ Database setup  
⏳ File storage  
⏳ Deployment  

**Как только backend будет готов → ВСЁ ЗАРАБОТАЕТ!** 🚀
